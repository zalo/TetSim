{
	"scripts": {
		"build": "esbuild ./src/main.js --bundle --minify --sourcemap --format=esm --outdir=./build"
	},
	"dependencies": {
		"esbuild": "^0.11.20",
		"three": "github:zalo/three.js#fix-webglmultiplerendertargets-add-options"
	}
}
